# 애플리케이션 보안 키
SECRET_KEY={{SECRET_KEY}}

# 데이터베이스 연결 설정
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=community
DB_PASSWORD={{DB_PASSWORD}}
DB_NAME=community

# 쿠키 및 세션 설정
COOKIE_SECURE=False
COOKIE_SAMESITE=lax
SESSION_TIMEOUT=86400

# CORS 설정 (프론트엔드 EC2 IP, 배포 시 자동 주입)
ALLOWED_ORIGINS={{FRONTEND_URL}}

# 디버그 모드
DEBUG=False
